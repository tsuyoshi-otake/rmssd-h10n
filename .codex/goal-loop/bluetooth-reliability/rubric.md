# Goal: Resolve Bluetooth reliability issues #9–#22
Workdir: C:/Codes/tsuyoshi-otake/rmssd-h10n
Max iterations: 8

The user approved implementation of all 11 defects and 3 improvements on 2026-09-08 JST. Existing issues #9–#22 are the tracking issues. Verification is performed directly by the implementing agent, as required by repository instructions.

## Criteria
- C1: Node BLE operations and one-shot capture finalize exactly once on success, timeout, disconnect and cancellation; late success cannot revive a retired session (#9–#12). Verify: `npm test` including scan/connect/attach/measurement fault injection. Expect: bounded settlement, no overlapping native connect for one device, late resources released, JSON emitted once after bounded cleanup.
- C2: RR filters reacquire after a long gap or sustained stable shift, and Node never records held values as new measurements (#18–#19). Verify: Node freshness/reacquisition tests and `:app:testDebugUnitTest` Rmssd tests. Expect: JS/Java matching finite recovery, spike rejection retained, no stale history points or measurement rows.
- C3: Android lifecycle is generation-scoped and control remains responsive during PFTP/bonding waits (#13–#14, #16). Verify: JVM lifecycle/control tests, Android compile. Expect: stale ACC timers do not stop a newer stream, stopped startup cannot create an SDK, blocked transfer cannot delay ACC OFF/stop; bounded workers and explicit cancellation ownership.
- C4: Recording recovery preserves foreign/uncertain recordings, tracks retry anchors and classifies local failures correctly (#20–#22). Verify: JVM recording-policy tests including timeout, definite rejection, local persistence failure and restart. Expect: unknown ownership never mutates a recording, uncertain anchors are quarantined, local failures never reset the bond, deletion follows durable persistence only.
- C5: Connection stage/reason/retry timing and bounded diagnostic events are exposed consistently (#15). Verify: status contract tests and rendered dashboard states. Expect: scanning/connecting/subscribing/retrying/needs-action distinguishable, bounded event retention and sanitized export.
- C6: The chosen H10 identifier is persisted and used across normal start/reconnect/service restart; Node supports explicit identity (#17). Verify: device-selection tests and dashboard interaction. Expect: two same-name devices do not redirect a saved selection, missing selection is visible, changing device serially releases the previous session.
- C7: Existing computation, history and application behavior remain valid. Verify: `npm test`, `npm --prefix app run build`, Capacitor copy, `gradlew :app:testDebugUnitTest :app:assembleDebug --no-daemon`, `git diff --check`. Expect: all exit 0 and generated assets agree with source. Dependency locks remain committed, any introduced versions are at least 7 days old and CI runs dependency audit.
- C8: Delivery is reviewable and test processes exit. Verify: final issue-to-change checklist, process listing for Node/Gradle/JVM test runners and workspace paths, git diff/status. Expect: every issue covered by implementation and evidence, no surviving test processes, no unrelated tracked changes, reusable verified learnings documented.
