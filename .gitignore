node_modules/

# Claude Code local runtime state (keep shareable settings only).
.claude/*
!.claude/settings.json

# Runtime output (CSV logs, status JSON, captured logs) — keep the dir, ignore contents.
data/*
!data/.gitkeep

*.log
.DS_Store

# Playwright MCP scratch (browser-test screenshots/snapshots)
.playwright-mcp/

# Developer-local editor configuration.
.vscode/
